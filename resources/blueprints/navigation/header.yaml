tabs:
  main:
    display: Main
    sections:
      -
        fields:
          -
            handle: icon
            field: common.icon
            config:
              width: 100
              instructions: Optional
          -
            handle: description
            field:
              display: Description
              type: text
          -
            handle: badge
            field:
              display: Badge
              type: text
              instructions: 'Highlight menu item for example with a "New" badge'
          -
            handle: two_col_menu
            field:
              display: 'Two column menu'
              type: toggle
              instructions: 'Used only in top level nav items that have children'
